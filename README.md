# NebulaDB
Nebula DB is a simple embedded Rust Vector Database.
Note that this is only a toy database.
Currently, the database use is limited.
In the future, it is planned that Nebula DB will replace Qdrant in the Doc_find text search engine.
